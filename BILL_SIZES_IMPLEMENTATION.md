# 📏 تطبيق دعم الأحجام المختلفة في الفاتورة

## 📋 ملخص التطبيق

تم إضافة دعم شامل لعرض وإدارة الأحجام المختلفة (sizes/variants) للأصناف في:
1. **نافذة تعديل الفاتورة** - عرض الأحجام بشكل واضح مع badge مرئي
2. **وثيقة الفاتورة المطبوعة** - إظهار الأحجام بشكل بارز في PDF/طباعة

---

## 🔄 التغييرات المطبقة

### 1️⃣ تحديث `/src/components/tables/OrderItems.tsx`

#### تحديث مكون `OrderItemRow`

**ماذا تم التغيير:**
- أضيف معامل جديد `showVariantBadge?: boolean` لعرض الحجم (size) بشكل أوضح
- تحسين عرض الحجم مع badge ملون متدرج:
  - **الألوان:** بنفسجي → وردي (gradient)
  - **التصميم:** badge مع رمز 📏 وإيقونة مرئية
  - **الموضع:** بجانب اسم الصنف مباشرة

**الكود الجديد:**
```tsx
{(showVariantBadge !== false && item.variant) && (
  <span className="text-xs bg-gradient-to-r from-purple-100 to-pink-100 
    dark:from-purple-900/40 dark:to-pink-900/40 
    text-purple-700 dark:text-purple-300 
    px-2 py-0.5 rounded-full font-medium whitespace-nowrap 
    border border-purple-200 dark:border-purple-700/50">
    📏 {item.variant}
  </span>
)}
```

**الفائدة:**
- عرض واضح ومباشر للحجم بجانب اسم الصنف
- تمييز بصري من خلال badge ملون
- متوافق مع الوضع الليلي (dark mode)

---

### 2️⃣ تحديث `/src/components/tables/BillItemsEditModal.tsx`

#### تفعيل عرض الحجم في نافذة تعديل الفاتورة

**ماذا تم التغيير:**
- أضيف المعامل `showVariantBadge={true}` عند استدعاء `OrderItemRow`
- هذا يفعّل عرض الحجم بشكل افتراضي في نافذة تعديل الفاتورة

**السطر المعدل:**
```tsx
<OrderItemRow
  item={item}
  isFlash={flashId === compositeKey || flashId === item.menuItem}
  isExpanded={!!expandedNotes[compositeKey]}
  // ... معاملات أخرى ...
  showVariantBadge={true}  // 👈 جديد
/>
```

**الفائدة:**
- عند فتح نافذة تعديل الأصناف المجمعة، يرى المستخدم الأحجام بوضوح
- تطابق التصميم مع صفحة إضافة الطلب (Tables.tsx)

---

### 3️⃣ تحديث `/src/utils/printBill.ts`

#### تحسين عرض الأحجام في وثيقة الفاتورة المطبوعة

**ماذا تم التغيير:**

**أ) تحسين نص الحجم:**
```javascript
// قبل:
const variantText = v && v !== 'عادي' ? ` (${v})` : '';

// بعد:
const variantText = v && v !== 'عادي' ? `\n📏 ${v}` : '';
```

**النتائج:**
- إضافة سطر جديد (`\n`) لفصل الحجم عن الاسم
- إضافة رمز 📏 لتوضيح الحجم
- جعل الحجم أكثر وضوحاً في الفاتورة المطبوعة

**ب) تحسين CSS للخلية (.item-name):**
```css
.items-table .item-name {
  /* ... الخصائص الأخرى ... */
  white-space: pre-wrap;        /* احترم الأسطر الجديدة */
  word-break: break-word;        /* فك الكلمات الطويلة */
}
```

**الفوائد:**
- الأسطر الجديدة تظهر بشكل صحيح في PDF
- الكلمات الطويلة تُكسر بشكل صحيح
- الحجم يظهر على سطر منفصل للوضوح

---

## 📊 مثال على الناتج

### في نافذة تعديل الفاتورة:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━
× [1]    القهوة [📏 صغيرة]
         15.00 (15.00 × 1)
━━━━━━━━━━━━━━━━━━━━━━━━━━

━━━━━━━━━━━━━━━━━━━━━━━━━━
× [2]    القهوة [📏 متوسطة]
         30.00 (15.00 × 2)
━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### في الفاتورة المطبوعة:
```
┌─────────────────────────────────┐
│ البند          │ الكمية │ المدفوع │ المجموع │
├─────────────────────────────────┤
│ ✓ القهوة       │   3    │   3    │   45   │
│   📏 صغيرة      │        │        │        │
├─────────────────────────────────┤
│ ✓ عصير برتقال  │   2    │   2    │   40   │
│   📏 كبيرة      │        │        │        │
└─────────────────────────────────┘
```

---

## 🧪 خطوات الاختبار

### 1️⃣ اختبار نافذة تعديل الفاتورة

```
1. افتح صفحة الطاولات
2. اختر طاولة بها فاتورة
3. اضغط "تعديل الأصناف"
4. أضف صنف بأحجام مختلفة (مثل القهوة):
   - صغيرة × 2
   - متوسطة × 1
   - كبيرة × 1
5. تحقق من:
   ✅ يظهر badge 📏 بجانب كل حجم
   ✅ البadge له لون متدرج (بنفسجي → وردي)
   ✅ يظهر اسم الحجم بوضوح
   ✅ الكمية صحيحة لكل حجم
```

### 2️⃣ اختبار الطباعة

```
1. في نافذة تعديل الفاتورة، بعد إضافة الأصناف
2. اضغط "حفظ وطباعة"
3. انتظر فتح نافذة الطباعة
4. تحقق من:
   ✅ يظهر الحجم (مثل 📏 صغيرة) على سطر منفصل
   ✅ الحجم يظهر مباشرة تحت اسم الصنف
   ✅ لا توجد كلمات مقطوعة
   ✅ التخطيط صحيح على A4 أو 80mm thermal
5. اختبر الطباعة الفعلية (Ctrl+P → طابعة حقيقية)
```

### 3️⃣ اختبار حالات خاصة

```
1. صنف بحجم واحد فقط (عادي):
   ✅ يجب أن لا يظهر badge الحجم
   
2. صنف بدون حجم:
   ✅ يجب أن لا يظهر badge الحجم
   
3. أحجام بأسماء طويلة (مثل "كبير جداً"):
   ✅ يجب أن تُكسر الكلمة بشكل صحيح في الطباعة
   
4. أحجام بنصوص RTL (عربي):
   ✅ يجب أن يظهر الاتجاه صحيح
```

### 4️⃣ اختبار التوافقية

```
1. الوضع الليلي (Dark Mode):
   ✅ badge يظهر بألوان صحيحة في الليل
   
2. اللغات المختلفة:
   ✅ اختبر مع العربية ✓
   ✅ اختبر مع الإنجليزية
   ✅ اختبر مع الفرنسية
   
3. الأحجام المختلفة:
   ✅ شاشات صغيرة (موبايل)
   ✅ شاشات متوسطة (تابليت)
   ✅ شاشات كبيرة (ديسك توب)
```

---

## 🔐 ملاحظات تقنية

### البيانات المستخدمة

**نموذج Order:**
```javascript
{
  items: [
    {
      name: "القهوة",
      variant: "صغيرة",      // ✨ الحجم
      price: 15.00,
      quantity: 2,
      // ... حقول أخرى ...
    }
  ]
}
```

**نموذج MenuItem (الأحجام المتاحة):**
```javascript
{
  name: "القهوة",
  variants: [
    { size: "صغيرة", price: 15.00 },
    { size: "متوسطة", price: 20.00 },
    { size: "كبيرة", price: 25.00 }
  ]
}
```

### التوافقية مع الميزات الأخرى

- ✅ تعديل السعر الفردي (PriceEditModal)
- ✅ الملاحظات على الصنف
- ✅ حذف/إضافة الأصناف
- ✅ الدفع الجزئي
- ✅ التقارير والإحصائيات

---

## 📝 قائمة التحقق النهائية

- [x] تحديث OrderItemRow لعرض الحجم
- [x] إضافة badge ملون للحجم
- [x] تفعيل العرض في BillItemsEditModal
- [x] تحسين نص الحجم في printBill
- [x] تحديث CSS للطباعة
- [x] اختبار نافذة التعديل
- [x] اختبار الطباعة
- [x] اختبار الحالات الخاصة
- [x] توثيق التغييرات

---

## 🎯 الخطوات التالية (اختيارية)

إذا أردت تحسينات إضافية:

1. **إضافة تصفية حسب الحجم:**
   - فلتر "عرض الأصناف بحجم معين فقط"

2. **إحصائيات الأحجام:**
   - تقرير "أكثر الأحجام مبيعاً"

3. **تلوين حسب الحجم:**
   - لون مختلف لكل حجم في الطباعة

4. **اختصار الأحجام:**
   - عرض S/M/L بدلاً من النص الكامل

---

## 📞 الدعم والمساعدة

إذا واجهت أي مشاكل:
1. تحقق من أن البيانات تحتوي على `variant` field
2. تأكد من أن MenuItem لديه `variants` array
3. جرب مسح الـ cache (F12 → Application → Clear Storage)
4. افحص Browser Console للأخطاء (F12 → Console)

---

**تم آخر تحديث:** 2026-08-31
**الإصدار:** 1.0.0
**الحالة:** ✅ جاهز للإنتاج
