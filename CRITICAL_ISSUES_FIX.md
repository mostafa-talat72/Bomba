# إصلاح المشاكل الحرجة في النظام

## المشاكل المكتشفة

### 1. ظهور الطلب في طاولة خاطئة
**المشكلة:** عند إضافة طلب على طاولة معينة، يظهر في طاولة أخرى عند فتحها.

**السبب المحتمل:** 
- مشكلة في cache الـ orders في AppContext
- عدم تحديث state بشكل صحيح عند تغيير الطاولة

**الحل:**
- التأكد من تحديث orders عند تغيير الطاولة المحددة
- إضافة key prop صحيح للـ components
- مسح cache عند تغيير الطاولة

### 2. عدم ظهور الفواتير القديمة
**الوضع الحالي:** 
- الـ backend لا يحتوي على date filter (تم إزالته بنجاح)
- الـ frontend يجلب جميع الفواتير بدون date filter

**التحقق المطلوب:**
- هل هناك limit في عدد الفواتير المجلوبة؟
- هل هناك pagination تمنع ظهور الفواتير القديمة؟

**الحل:**
- زيادة الـ limit في `getBills()` إلى رقم أكبر
- أو إزالة الـ pagination للفواتير غير المدفوعة

### 3. الطاولات لا تظهر محجوزة
**المشكلة:** إذا كانت الفاتورة غير مدفوعة بالكامل ولكن قديمة، لا تظهر الطاولة محجوزة.

**السبب:** الفواتير القديمة لا تُجلب (مشكلة #2)

**الحل:** حل مشكلة #2 سيحل هذه المشكلة تلقائياً

### 4. الطلبات لا تظهر
**المشكلة:** الطلبات المرتبطة بالفواتير القديمة غير المدفوعة لا تظهر.

**السبب:** الفواتير القديمة لا تُجلب (مشكلة #2)

**الحل:** حل مشكلة #2 سيحل هذه المشكلة تلقائياً

## الإجراءات المطلوبة

### الخطوة 1: فحص الـ limit في getBills
```typescript
// في src/context/AppContext.tsx
const fetchBills = async (): Promise<void> => {
  const response = await api.getBills({ limit: 1000 }); // زيادة الـ limit
  // ...
}
```

### الخطوة 2: إصلاح مشكلة cache الطلبات
- إضافة useEffect لمسح الطلبات عند تغيير الطاولة
- التأكد من أن الطلبات مرتبطة بالطاولة الصحيحة

### الخطوة 3: التحقق من الفواتير المجلوبة
- إضافة console.log لعرض عدد الفواتير المجلوبة
- التحقق من تواريخ الفواتير

## الحالة
- ⏳ قيد التحقيق
- 🔍 يحتاج مزيد من المعلومات من المستخدم
