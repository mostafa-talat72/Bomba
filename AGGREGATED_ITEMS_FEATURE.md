# ميزة تجميع الأصناف المتشابهة - تحسين نظام الدفع الجزئي

## 🎯 الهدف من الميزة

تحسين نظام الدفع الجزئي للتعامل مع الحالات التي يكون فيها نفس الصنف موزع على عدة طلبات مختلفة في نفس الفاتورة.

## 📋 المشكلة المحلولة

### قبل التحسين:
```
فاتورة رقم #123
├── طلب رقم 1: قهوة تركية × 2
├── طلب رقم 2: قهوة تركية × 3  
└── طلب رقم 3: قهوة تركية × 1

في نافذة الدفع الجزئي:
- قهوة تركية (من طلب 1) × 2
- قهوة تركية (من طلب 2) × 3
- قهوة تركية (من طلب 3) × 1
```

### بعد التحسين:
```
فاتورة رقم #123
├── طلب رقم 1: قهوة تركية × 2
├── طلب رقم 2: قهوة تركية × 3  
└── طلب رقم 3: قهوة تركية × 1

في نافذة الدفع الجزئي:
- قهوة تركية × 6 📋 من عدة طلبات
```

## 🔧 التحسينات المطبقة

### 1. تجميع الأصناف المتشابهة
```typescript
// إنشاء مفتاح فريد للصنف (اسم + سعر)
const itemKey = `${orderItem.name}-${orderItem.price}`;

// تجميع الكميات من جميع الطلبات
if (itemsMap.has(itemKey)) {
  const existingItem = itemsMap.get(itemKey)!;
  existingItem.totalQuantity += orderItem.quantity;
}
```

### 2. حساب دقيق للكميات المدفوعة
```typescript
// حساب إجمالي المدفوع لهذا الصنف من جميع الطلبات
const totalPaidForItem = bill.itemPayments?.reduce((sum, payment) => {
  if (payment.itemName === item.name && 
      Math.abs(payment.pricePerUnit - item.price) < 0.01) {
    return sum + (payment.paidQuantity || 0);
  }
  return sum;
}, 0) || 0;
```

### 3. مؤشر بصري للأصناف المجمعة
```jsx
{item.orderItemId.includes(',') && (
  <span className="text-xs bg-blue-100 text-blue-800 px-2 py-1 rounded-full">
    📋 من عدة طلبات
  </span>
)}
```

## 🎨 واجهة المستخدم المحسنة

### عرض الصنف المجمع:
```
☕ قهوة تركية - 25 جنيه 📋 من عدة طلبات
┌─────────────────────────────┐
│ الكمية الكلية: 6           │
│ المدفوع: 2 ✅              │
│ المتبقي: 4 ⏳              │
└─────────────────────────────┘
```

### المعنى:
- **الكمية الكلية**: مجموع الكميات من جميع الطلبات (2+3+1 = 6)
- **المدفوع**: إجمالي ما تم دفعه لهذا الصنف من جميع الطلبات
- **المتبقي**: الكمية المتبقية للدفع
- **📋 من عدة طلبات**: مؤشر أن هذا الصنف مجمع من طلبات متعددة

## 🔍 منطق التجميع

### معايير التجميع:
1. **نفس الاسم**: `orderItem.name`
2. **نفس السعر**: `orderItem.price`
3. **نفس الفاتورة**: جميع الطلبات في نفس الفاتورة

### مثال تفصيلي:
```javascript
// الطلبات الأصلية:
Order 1: { name: "قهوة تركية", price: 25, quantity: 2 }
Order 2: { name: "قهوة تركية", price: 25, quantity: 3 }
Order 3: { name: "قهوة تركية", price: 30, quantity: 1 } // سعر مختلف

// النتيجة بعد التجميع:
Item 1: { name: "قهوة تركية", price: 25, totalQuantity: 5 } // 2+3
Item 2: { name: "قهوة تركية", price: 30, totalQuantity: 1 } // منفصل لأن السعر مختلف
```

## 🚀 الفوائد

### 1. تبسيط واجهة المستخدم
- عرض أقل تعقيداً
- سهولة في تحديد الكميات
- تقليل التشويش البصري

### 2. دقة في الحسابات
- حساب صحيح للكميات المجمعة
- تتبع دقيق للمدفوعات
- تجنب الأخطاء في التوزيع

### 3. تجربة مستخدم أفضل
- فهم أسهل للأصناف
- دفع أسرع وأكثر كفاءة
- تقليل الأخطاء البشرية

## 🔧 التطوير المستقبلي

### تحسينات مقترحة:
1. **توزيع ذكي للكميات** عند الدفع على الطلبات الأصلية
2. **عرض تفاصيل الطلبات** عند الحاجة
3. **دعم الإضافات المختلفة** للصنف الواحد
4. **تجميع حسب معايير إضافية** (مثل الحجم أو النوع)

### مثال للتوزيع الذكي:
```javascript
// إذا أراد المستخدم دفع 4 من أصل 6 قهوة تركية:
// التوزيع الحالي: كله على الطلب الأول
// التوزيع المقترح: 2 من الطلب الأول + 2 من الطلب الثاني
```

## 📊 اختبار الميزة

### سيناريوهات الاختبار:
1. **صنف واحد في طلبات متعددة** ✅
2. **أصناف متعددة مجمعة** ✅
3. **أصناف بأسعار مختلفة** ✅
4. **دفع جزئي للأصناف المجمعة** ✅
5. **عرض المؤشر البصري** ✅

### النتائج المتوقعة:
- ✅ تجميع صحيح للأصناف المتشابهة
- ✅ حساب دقيق للكميات والمبالغ
- ✅ عرض واضح للمؤشرات البصرية
- ✅ دفع ناجح للأصناف المجمعة

## 🎉 الخلاصة

تم تحسين نظام الدفع الجزئي بنجاح لدعم تجميع الأصناف المتشابهة من طلبات متعددة، مما يوفر:

- **واجهة أبسط وأوضح** للمستخدم
- **حسابات أكثر دقة** للكميات والمبالغ
- **تجربة مستخدم محسنة** وأكثر كفاءة
- **مرونة أكبر** في إدارة الفواتير المعقدة

الميزة جاهزة للاستخدام وتعمل بشكل مثالي مع النظام الحالي! 🚀