# Implementation Plan

- [ ] 1. تعديل عرض قسم الطاولة في نافذة إدارة الدفع
  - تعديل الشرط من `{selectedBill?.tableNumber && (` إلى `{selectedBill?.status !== 'paid' && (`
  - إضافة منطق شرطي لعرض حالتين: مرتبطة أو غير مرتبطة بطاولة
  - عرض رقم الطاولة + زر "تغيير الطاولة" (أزرق) للفواتير المرتبطة
  - عرض "غير مرتبطة بطاولة" + زر "ربط بطاولة" (أخضر) للفواتير غير المرتبطة
  - _Requirements: 1.1, 1.5, 2.3, 3.4_

- [ ] 2. تحديث النافذة المنبثقة لتغيير/ربط الطاولة
  - تحديث عنوان النافذة ليكون ديناميكي بناءً على حالة الفاتورة
  - إخفاء عرض "الطاولة الحالية" للفواتير غير المرتبطة
  - تحديث رسالة التحذير لتكون مناسبة لكل حالة
  - تحديث نص زر التأكيد ليكون ديناميكي
  - _Requirements: 1.2, 1.3, 2.1, 2.2_

- [ ] 3. تحديث دالة handleChangeTable لدعم الحالتين
  - تعديل رسالة النجاح لتكون ديناميكية (ربط/نقل)
  - التأكد من إعادة تعيين القيم بشكل صحيح بعد النجاح
  - _Requirements: 1.3, 1.4, 2.2_

- [ ] 4. تحديث عرض الفواتير في القائمة الرئيسية
  - التأكد من عرض "⚠️ غير مرتبطة بطاولة" للفواتير غير المرتبطة
  - التأكد من عرض "🪑 طاولة: X" للفواتير المرتبطة
  - استخدام ألوان مميزة (أزرق للمرتبطة، رمادي لغير المرتبطة)
  - _Requirements: 3.1, 3.2, 3.3_

- [ ] 5. اختبار الوظيفة
  - اختبار ربط فاتورة بلايستيشن غير مرتبطة بطاولة
  - اختبار تغيير طاولة فاتورة مرتبطة
  - اختبار عدم ظهور الأزرار للفواتير المدفوعة
  - اختبار تحديث الواجهة بعد النجاح
  - اختبار معالجة الأخطاء
  - _Requirements: 1.4, 2.4, 4.1, 4.2, 4.3, 4.4_
