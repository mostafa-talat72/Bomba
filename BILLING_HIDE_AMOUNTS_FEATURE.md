# ميزة إخفاء/إظهار المبالغ في صفحة الفواتير وتقرير الاستهلاك

## الميزة الجديدة

### في صفحة الفواتير:
تم إضافة ميزة إخفاء/إظهار الأرقام في بطاقات الإحصائيات:
- **المبلغ المحصل** - يمكن إخفاؤه/إظهاره
- **المبلغ المتبقي** - يمكن إخفاؤه/إظهاره

### في صفحة تقرير الاستهلاك:
تم إضافة ميزة إخفاء/إظهار:
- **إجمالي المبيعات** - يمكن إخفاؤه/إظهاره

## السلوك الافتراضي

- عند فتح الصفحة، الأرقام **مخفية** (تظهر كـ `••••••`)
- عند الضغط على أيقونة العين، يتم **إظهار** الرقم
- عند الضغط مرة أخرى، يتم **إخفاء** الرقم

## التغييرات المنفذة

### 1. إضافة Import لأيقونة `EyeOff`

```typescript
import { 
  Receipt, QrCode, Printer, DollarSign, CreditCard, Calendar, 
  User, CheckCircle, Table as TableIcon, Search, X, 
  Eye, EyeOff, // ← EyeOff جديد
  Gamepad2, ChevronDown, ChevronUp 
} from 'lucide-react';
```

### 2. إضافة State للإخفاء/الإظهار

```typescript
const [showPaidAmount, setShowPaidAmount] = useState(false); // افتراضياً: مخفي
const [showRemainingAmount, setShowRemainingAmount] = useState(false); // افتراضياً: مخفي
```

### 3. تحديث بطاقة "المبلغ المحصل"

```typescript
<div className="bg-white dark:bg-gray-800 rounded-lg shadow-sm border border-gray-200 dark:border-gray-700 p-4 sm:p-6">
  <div className="flex items-center justify-between">
    <div className="flex items-center">
      <div className="w-10 h-10 sm:w-12 sm:h-12 bg-green-100 dark:bg-green-900 rounded-lg flex items-center justify-center">
        <DollarSign className="h-5 w-5 sm:h-6 sm:w-6 text-green-600 dark:text-green-400" />
      </div>
      <div className="mr-3 sm:mr-4">
        <p className="text-xs sm:text-sm font-medium text-gray-600 dark:text-gray-300">
          المبلغ المحصل
        </p>
        <p className="text-xl sm:text-2xl font-bold text-green-600 dark:text-green-400">
          {showPaidAmount ? formatCurrency(billStats.totalPaid) : '••••••'}
        </p>
      </div>
    </div>
    <button
      onClick={() => setShowPaidAmount(!showPaidAmount)}
      className="p-2 hover:bg-gray-100 dark:hover:bg-gray-700 rounded-lg transition-colors"
      title={showPaidAmount ? 'إخفاء المبلغ' : 'إظهار المبلغ'}
    >
      {showPaidAmount ? (
        <EyeOff className="h-5 w-5 text-gray-500 dark:text-gray-400" />
      ) : (
        <Eye className="h-5 w-5 text-gray-500 dark:text-gray-400" />
      )}
    </button>
  </div>
</div>
```

### 4. تحديث بطاقة "المبلغ المتبقي"

```typescript
<div className="bg-white dark:bg-gray-800 rounded-lg shadow-sm border border-gray-200 dark:border-gray-700 p-4 sm:p-6">
  <div className="flex items-center justify-between">
    <div className="flex items-center">
      <div className="w-10 h-10 sm:w-12 sm:h-12 bg-yellow-100 dark:bg-yellow-900 rounded-lg flex items-center justify-center">
        <DollarSign className="h-5 w-5 sm:h-6 sm:w-6 text-yellow-600 dark:text-yellow-400" />
      </div>
      <div className="mr-3 sm:mr-4">
        <p className="text-xs sm:text-sm font-medium text-gray-600 dark:text-gray-300">
          المبلغ المتبقي
        </p>
        <p className="text-xl sm:text-2xl font-bold text-red-600 dark:text-red-400">
          {showRemainingAmount ? formatCurrency(billStats.totalRemaining) : '••••••'}
        </p>
      </div>
    </div>
    <button
      onClick={() => setShowRemainingAmount(!showRemainingAmount)}
      className="p-2 hover:bg-gray-100 dark:hover:bg-gray-700 rounded-lg transition-colors"
      title={showRemainingAmount ? 'إخفاء المبلغ' : 'إظهار المبلغ'}
    >
      {showRemainingAmount ? (
        <EyeOff className="h-5 w-5 text-gray-500 dark:text-gray-400" />
      ) : (
        <Eye className="h-5 w-5 text-gray-500 dark:text-gray-400" />
      )}
    </button>
  </div>
</div>
```

## كيف يعمل

### الحالة الافتراضية (مخفي):
```
┌─────────────────────────────┐
│ 💵 المبلغ المحصل            │
│    ••••••              👁    │
└─────────────────────────────┘
```

### بعد الضغط على العين (ظاهر):
```
┌─────────────────────────────┐
│ 💵 المبلغ المحصل            │
│    15,250 ج.م          👁‍🗨  │
└─────────────────────────────┘
```

## المزايا

### ✅ خصوصية محسّنة
- إخفاء المبالغ المالية عند عدم الحاجة
- مفيد عند عرض الشاشة أمام الآخرين
- حماية المعلومات المالية الحساسة

### ✅ تحكم مستقل
- كل بطاقة لها زر إخفاء/إظهار خاص
- يمكن إظهار المبلغ المحصل وإخفاء المتبقي (أو العكس)
- مرونة كاملة في التحكم

### ✅ تجربة مستخدم سلسة
- تبديل فوري بدون تأخير
- أيقونات واضحة (Eye/EyeOff)
- Tooltip يوضح الإجراء
- Hover effect على الزر

### ✅ تصميم متناسق
- يتماشى مع تصميم البطاقات الحالي
- دعم Dark Mode
- Responsive على جميع الشاشات

## الأيقونات المستخدمة

### Eye (عين مفتوحة):
- تظهر عندما يكون المبلغ **مخفي**
- معناها: "اضغط لإظهار المبلغ"

### EyeOff (عين مغلقة):
- تظهر عندما يكون المبلغ **ظاهر**
- معناها: "اضغط لإخفاء المبلغ"

## الألوان والتصميم

### الزر:
```css
className="p-2 hover:bg-gray-100 dark:hover:bg-gray-700 rounded-lg transition-colors"
```

### الأيقونة:
```css
className="h-5 w-5 text-gray-500 dark:text-gray-400"
```

### المبلغ المخفي:
```
••••••
```
- 6 نقاط (bullets)
- نفس حجم الخط الأصلي
- نفس اللون الأصلي

## حالات الاستخدام

### 1. عرض الشاشة في مكان عام:
```
المستخدم يفتح صفحة الفواتير
→ الأرقام مخفية افتراضياً
→ لا أحد يرى المبالغ المالية
```

### 2. مراجعة الإحصائيات:
```
المستخدم يريد رؤية الأرقام
→ يضغط على أيقونة العين
→ تظهر الأرقام
→ يراجع الإحصائيات
→ يضغط مرة أخرى لإخفائها
```

### 3. عرض جزئي:
```
المستخدم يريد رؤية المبلغ المحصل فقط
→ يضغط على العين في بطاقة "المبلغ المحصل"
→ يترك "المبلغ المتبقي" مخفياً
```

## الملفات المعدلة

1. `src/pages/Billing.tsx`:
   - إضافة import لـ `EyeOff`
   - إضافة states: `showPaidAmount`, `showRemainingAmount`
   - تحديث بطاقة "المبلغ المحصل"
   - تحديث بطاقة "المبلغ المتبقي"

## الاختبار

### اختبار 1: الحالة الافتراضية
```bash
1. افتح صفحة الفواتير
2. تحقق من أن المبلغ المحصل مخفي (••••••)
3. تحقق من أن المبلغ المتبقي مخفي (••••••)
4. تحقق من وجود أيقونة Eye في كلا البطاقتين
```

### اختبار 2: إظهار المبلغ
```bash
1. اضغط على أيقونة Eye في بطاقة "المبلغ المحصل"
2. تحقق من ظهور المبلغ الفعلي
3. تحقق من تغيير الأيقونة إلى EyeOff
4. تحقق من أن "المبلغ المتبقي" لا يزال مخفياً
```

### اختبار 3: إخفاء المبلغ
```bash
1. بعد إظهار المبلغ، اضغط على أيقونة EyeOff
2. تحقق من إخفاء المبلغ مرة أخرى (••••••)
3. تحقق من تغيير الأيقونة إلى Eye
```

### اختبار 4: التحكم المستقل
```bash
1. أظهر "المبلغ المحصل"
2. أظهر "المبلغ المتبقي"
3. أخفِ "المبلغ المحصل" فقط
4. تحقق من أن "المبلغ المتبقي" لا يزال ظاهراً
```

### اختبار 5: Dark Mode
```bash
1. فعّل الوضع الداكن
2. تحقق من ألوان الأيقونات
3. تحقق من hover effect
4. تحقق من وضوح المبالغ المخفية
```

## ملاحظات

### الخصوصية:
- الأرقام مخفية افتراضياً لحماية الخصوصية
- لا يتم حفظ الحالة (عند إعادة تحميل الصفحة، تعود للحالة الافتراضية)

### الأداء:
- تبديل فوري بدون تأخير
- لا تأثير على أداء الصفحة
- State محلي فقط

### التوافق:
- يعمل على جميع المتصفحات
- Responsive على جميع الشاشات
- دعم كامل لـ Dark Mode

🚀 **الميزة جاهزة للاستخدام!**


---

## تقرير الاستهلاك

### التغييرات المنفذة

#### 1. إضافة Imports

```typescript
import {
  ShoppingCartOutlined,
  CoffeeOutlined,
  FireOutlined,
  ShoppingFilled,
  ReloadOutlined,
  DownloadOutlined,
  FilePdfOutlined,
  CalendarOutlined,
  BarChartOutlined,
  ExclamationCircleOutlined,
  PrinterOutlined,
  EyeOutlined,        // ← جديد
  EyeInvisibleOutlined // ← جديد
} from '@ant-design/icons';
```

#### 2. إضافة State

```typescript
const [showTotalSales, setShowTotalSales] = useState(false); // افتراضياً: مخفي
```

#### 3. تحديث عرض "إجمالي المبيعات"

```typescript
<div className="text-center">
  <div className="flex items-center justify-center gap-3">
    <div className="text-3xl font-bold text-blue-600 mb-1">
      {showTotalSales ? `${toArabicNumbers(formatNumber(totalSales))} ج.م` : '••••••'}
    </div>
    <Button
      type="text"
      icon={showTotalSales ? <EyeInvisibleOutlined /> : <EyeOutlined />}
      onClick={() => setShowTotalSales(!showTotalSales)}
      title={showTotalSales ? 'إخفاء المبلغ' : 'إظهار المبلغ'}
      className="hover:bg-gray-100"
    />
  </div>
  <div className="text-gray-500 text-sm">إجمالي المبيعات</div>
</div>
```

### كيف يعمل

#### الحالة الافتراضية (مخفي):
```
┌─────────────────────────────┐
│      ••••••          👁      │
│   إجمالي المبيعات           │
└─────────────────────────────┘
```

#### بعد الضغط على العين (ظاهر):
```
┌─────────────────────────────┐
│  ١٥,٢٥٠ ج.م        👁‍🗨     │
│   إجمالي المبيعات           │
└─────────────────────────────┘
```

### الملفات المعدلة

1. `src/pages/ConsumptionReport.tsx`:
   - إضافة imports: `EyeOutlined`, `EyeInvisibleOutlined`
   - إضافة state: `showTotalSales`
   - تحديث عرض "إجمالي المبيعات"

---

## الملخص النهائي

### الصفحات المحدثة:
1. ✅ **صفحة الفواتير** - إخفاء/إظهار المبلغ المحصل والمتبقي
2. ✅ **تقرير الاستهلاك** - إخفاء/إظهار إجمالي المبيعات

### الملفات المعدلة:
1. `src/pages/Billing.tsx`
2. `src/pages/ConsumptionReport.tsx`
3. `BILLING_HIDE_AMOUNTS_FEATURE.md`

🚀 **الميزة متاحة الآن في صفحتين!**
