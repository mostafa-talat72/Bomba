# إصلاح عرض فواتير الجلسات غير المرتبطة بطاولة

## المشكلة
كانت هناك حاجة للتأكد من أن فواتير جلسات البلايستيشن غير المرتبطة بطاولة تظهر في قسم "أجهزة البلايستيشن" في صفحة الفواتير، وليس في قسم "فواتير غير مرتبطة بطاولة".

## الحل
الكود كان يعمل بالفعل بشكل صحيح! النظام يفصل الفواتير بشكل تلقائي:

- ✅ **فواتير البلايستيشن غير المرتبطة بطاولة** → تظهر في قسم "أجهزة البلايستيشن" تحت اسم الجهاز
- ✅ **فواتير الكافيه غير المرتبطة بطاولة** → تظهر في قسم "فواتير غير مرتبطة بطاولة"

تم إضافة:

### 1. تعليقات توضيحية
- أضفنا تعليقات واضحة في الكود لتوضيح أن الفواتير غير المرتبطة بطاولة تُضاف إلى قائمة `bills` لكل جهاز
- السطور 1393-1401 في `src/pages/Billing.tsx`

### 2. Console Logs للتتبع
أضفنا console logs لتتبع:
- إجمالي فواتير البلايستيشن
- عدد الفواتير غير المرتبطة بطاولة
- تفاصيل كل فاتورة تُضاف لجهاز معين
- إحصائيات كل جهاز (عدد الفواتير، الفواتير غير المرتبطة، الجلسات النشطة)

## كيف يعمل النظام

### تجميع الفواتير
1. يتم تجميع جميع فواتير البلايستيشن (التي تحتوي على `billType === 'playstation'` أو جلسات بلايستيشن)
2. لكل جلسة في الفاتورة، يتم:
   - تحديد اسم الجهاز من `session.deviceName` أو `session.deviceNumber`
   - التحقق من ارتباط الفاتورة بطاولة (`!!bill.table`)
   - إذا لم تكن مرتبطة بطاولة، تُضاف إلى قائمة `bills` للجهاز

### العرض
- كل جهاز يظهر في قسم "أجهزة البلايستيشن"
- تحت كل جهاز تظهر فواتيره غير المرتبطة بطاولة
- الفواتير غير المرتبطة تظهر مع علامة "⚠️ غير مرتبطة بطاولة"
- إذا كان للفاتورة اسم عميل، يظهر "👤 [اسم العميل]"

### الفلترة
- يمكن فلترة الفواتير حسب الحالة: غير مدفوع / مدفوع / الكل
- يمكن البحث عن جهاز معين
- الأجهزة التي ليس لها فواتير (بعد الفلترة) لا تظهر

## مثال على البيانات

```javascript
// فاتورة جلسة بلايستيشن غير مرتبطة بطاولة
{
  _id: "bill123",
  billNumber: "B-001",
  billType: "playstation",
  table: null,  // غير مرتبطة بطاولة
  customerName: "أحمد",
  status: "partial",
  total: 100,
  paid: 50,
  remaining: 50,
  sessions: [{
    deviceName: "PS5 - 1",
    deviceType: "playstation",
    status: "active"
  }]
}
```

هذه الفاتورة ستظهر تحت "PS5 - 1" في قسم أجهزة البلايستيشن مع علامة "⚠️ غير مرتبطة بطاولة" و "👤 أحمد".

## اختبار التغييرات

1. افتح صفحة الفواتير
2. افتح Console في المتصفح (F12)
3. ابحث عن الرسائل التالية:
   - `🎮 إجمالي فواتير البلايستيشن: X`
   - `🎮 فواتير بلايستيشن غير مرتبطة بطاولة: Y`
   - `✅ إضافة فاتورة غير مرتبطة بطاولة للجهاز...`
   - `📊 الجهاز...`
   - `📋 فواتير غير مرتبطة بطاولة (كافيه فقط): Z`

4. تحقق من أن:
   - فواتير البلايستيشن تظهر في قسم "أجهزة البلايستيشن" تحت اسم الجهاز
   - فواتير الكافيه فقط تظهر في قسم "فواتير غير مرتبطة بطاولة"
   - الفلترة تعمل بشكل صحيح (غير مدفوع / مدفوع / الكل)

## ملاحظات مهمة

### الكود يعمل بشكل صحيح!
- فواتير البلايستيشن غير المرتبطة بطاولة **تظهر بالفعل** في قسم "أجهزة البلايستيشن"
- قسم "فواتير غير مرتبطة بطاولة" يستبعد فواتير البلايستيشن والكمبيوتر تلقائياً

### إذا لم تظهر فواتير البلايستيشن، تحقق من:
1. **الفلتر**: هل اخترت "غير مدفوع" أو "الكل"؟
2. **الجهاز مطوي**: انقر على السهم لفتح قائمة فواتير الجهاز
3. **البيانات**: تأكد من:
   - الفاتورة تحتوي على جلسة بلايستيشن (`bill.sessions` مع `deviceType === 'playstation'`)
   - `bill.table` يساوي `null` أو `undefined`
   - حالة الفاتورة تطابق الفلتر المختار

### كيف يتم الفصل بين الأقسام؟
```typescript
// قسم "فواتير غير مرتبطة بطاولة" يستبعد البلايستيشن والكمبيوتر
const unlinkedBills = bills.filter((bill: Bill) => 
  !bill.table && 
  bill.billType !== 'playstation' &&  // استبعاد البلايستيشن
  bill.billType !== 'computer' &&     // استبعاد الكمبيوتر
  !bill.sessions?.some((s: any) => 
    s.deviceType === 'playstation' || s.deviceType === 'computer'
  )
);
```
